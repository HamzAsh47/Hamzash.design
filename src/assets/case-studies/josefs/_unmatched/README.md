# Not on the page

Files here are kept but never bundled — the figure glob only looks one level
deep, so a nested folder is out of its reach.

`NEXT LEVEL ... Europa Passage ....mp4` landed here because its name matches no
slot in `caseStudies.ts`. It was still being shipped to every visitor at 4.3 MB
while rendering nowhere.

To publish it, move it back up one level and rename it to a slot — the clip is
the Europa Passage second location, so `storefront-3.mp4` would stack it after
the two storefront stills. Then add its alt text in `caseStudies.ts`.
