# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env.local

# Wrangler local dev state (miniflare caches, trace store)
.wrangler/

# Local Worker secrets (Resend key). Never committed — the real one lives in
# `wrangler secret put`, and this file only exists to run the Worker locally.
.dev.vars
.dev.vars.*
